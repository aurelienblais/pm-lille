%section.content-header
%section.content
  .container-fluid
    .row
      .col-12
        .card
          .card-header
            %h3.card-title Grades
            .card-tools
              = link_to new_rank_path, remote: true, class: 'btn btn-sm btn-secondary' do
                Nouveau grade
          .card-body
            %table.table.table-bordered.table-hover.text-center
              %thead
                %tr
                  %th #
                  %th Nom
                  %th Nombre d'agents
                  %th
              %tbody#items-table= render items